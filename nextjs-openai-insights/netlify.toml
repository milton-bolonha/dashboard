[build]
  command = "npm run build"
  publish = ".next"

[dev]
  command = "npm run dev"
  framework = "next"
  targetPort = 3000

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

