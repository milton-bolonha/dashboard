# === Public URLs / Function Endpoints ===
# URL completa do endpoint de geração (tem prioridade máxima).
# Ex.: https://aisalesnow.netlify.app/.netlify/functions/ai-generate
NEXT_PUBLIC_GENERATE_ENDPOINT=

# Base URL do ambiente atual (Vercel ou Netlify). Use para custom domain.
# Ex.: https://dashmasterpro.vercel.app
NEXT_PUBLIC_GENERATE_BASE_URL=

# Base URL para Vercel (se preferir separar por ambiente).
# Ex.: https://dashmasterpro.vercel.app
NEXT_PUBLIC_VERCEL_FUNCTIONS_BASE_URL=

# Base URL para Netlify (deploy oficial Netlify).
# Ex.: https://aisalesnow.netlify.app
NEXT_PUBLIC_FUNCTIONS_BASE_URL=

# URL pública padrão do app (fallback genérico).
# Ex.: https://aisalesnow.netlify.app
NEXT_PUBLIC_APP_URL=


# === OpenAI ===
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini
OPENAI_MAX_OUTPUT_TOKENS=600
OPENAI_TEMPERATURE=0.7


# === Rate limiting da função Netlify ===
# Quantidade de requisições por IP dentro da janela (em segundos).
FUNCTION_RATE_LIMIT=30

