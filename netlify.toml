[build]
command = "cd dashboard && npm run build && mkdir -p .next/templates && cp -r templates .next/"
publish = "dashboard/.next"

[build.environment]
NODE_VERSION = "22.16.0"

[[plugins]]
package = "@netlify/plugin-nextjs"
